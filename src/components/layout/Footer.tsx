export function Footer() {
  return (
    <footer className="border-t border-border bg-[color:var(--surface)]/80 px-4 py-6 text-xs text-[color:var(--text-muted)] sm:px-6 lg:px-8">
      <p>
        Content owned by respective coalition entities. Entities may remove authored content; contribution
        metrics remain as historical data points.
      </p>
    </footer>
  );
}
